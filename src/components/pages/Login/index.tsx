import { LoginForm } from "./login-form";

const LoginPage = () => {
    return (
        <main className="max-w-3xl mx-auto flex items-center justify-center min-h-screen">
            <LoginForm />
        </main>
    );
};

export default LoginPage;
